using UnityEngine;
using System.Collections;

public class ObjectSelector : MonoBehaviour {


	void Start () {
		
	}

	// Update is called once per frame
	void Update () {

	}

	void OnMouseDown(){
		//		if(totalPlants <= 1 && zombieEntered == false ){
		//			bool result = plantSpawner.GetComponent<PlantSpawner>().SpawnHere(transform.position);
//		plantSpawner.GetComponent<PlantSpawner>().SpawnHere(transform.position);
		//			if(result == true){
		//				totalPlants = totalPlants + 1;
		//			}
		//		}
	}
	void changePrefab(){
		
	}

}
